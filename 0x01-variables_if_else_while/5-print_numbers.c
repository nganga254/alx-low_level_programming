#include <stdio.h>

/**
* main - Entry point, the function returns (0)
*
* description :  print decima numbers
*
* return 0(Always)
*/

int main(void)
{
	int x;
	for (x = 0 ; x <= 9 ; x++)
	{
		printf("%d", x);
	}
	printf("\n");
return (0);
}
