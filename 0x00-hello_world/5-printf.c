#include <stdio.h>

/* The main function is the entry point of the program
 * It returns a value zero or EOF
 */
int main(void)
{
	printf ("with proper grammar, but the outcome is a piece of art,");
	return(0);
}
